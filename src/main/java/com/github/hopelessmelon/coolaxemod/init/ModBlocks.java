/*
 This file is the same as the ModItems file, but for blocks. The tutorial has run out so I need to find another or figure this out on my own.
 Hopefully it's pretty similar to the process with Items and I can just follow the same steps here.
 7/1: I don't think I actually need this.
 */

package com.github.hopelessmelon.coolaxemod.init;

import com.github.hopelessmelon.coolaxemod.CoolAxeMod;
import net.minecraft.block.Block;
import net.minecraftforge.registries.ObjectHolder;

@ObjectHolder(CoolAxeMod.MODID)
public class ModBlocks {
    public static final Block EXAMPLE_BLOCK = null;
}
